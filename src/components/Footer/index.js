import styles from "./Footer.module.css"
function Footer(){
    return(
        <footer className={styles.footer}>
            <h2>Movies &copy; Desenvolvido por Eduardo Moura</h2>
        </footer>

    );
}

export default Footer;